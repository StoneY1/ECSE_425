
To run the pipelined processor:

1. Convert your MIPS code to machine code
2. Replace program.txt with your machine code file (make sure to name your file program.txt)
3. In ModelSIM, run "source testbench.tcl"
4. The final register values can be seen in "register_file.txt", memory can be seen in "memory_file.txt"